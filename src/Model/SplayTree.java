package Model;

import Model.SplayTreeNode;



public class SplayTree<T> {
	
	
	public void add(SplayTreeNode<T> pNode) {
		Comparable<SplayTreeNode<T>> compNode = (Comparable<SplayTreeNode<T>>) pNode;
	}
}
